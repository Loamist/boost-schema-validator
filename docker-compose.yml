services:
  app:
    image: node:23
    working_dir: /app
    command: npm run dev -- --host 0.0.0.0
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    tty: true

volumes:
  node_modules:
